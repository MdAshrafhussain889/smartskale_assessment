# Local dev only — change passwords if you expose Judge0 publicly
REDIS_PASSWORD=hiremind_judge0_redis_dev
POSTGRES_PASSWORD=hiremind_judge0_pg_dev
POSTGRES_USER=judge0
POSTGRES_DB=judge0
REDIS_HOST=redis
POSTGRES_HOST=db
